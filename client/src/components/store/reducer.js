const initialState = {
   name: "",
   email: "",
   id: "",
   image_url: "",
   background: "",
   income: "",
   meditations: [],
}

export function teacherReducer(state = initialState, action) {
   switch (action.type) {
      default:
         return state
   }
}
